#include <iostream>
using namespace std;
 
int main() {
	string str;
	getline(cin, str);
	for(int i = 0; i < str.length(); i++){
		if(str[i]>='0' && str[i]<'9'){
			str[i]++;
		}
		else if(str[i]=='9') str[i]='0';
	}
	cout << str;
	return 0;
}
